{
  "name": "cypress-api-test",
  "version": "1.0.0",
  "description": "API test for POST /api/v1/assessment",
  "scripts": {
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.17.4",
    "cypress-mochawesome-reporter": "^4.0.0"
  }
}
